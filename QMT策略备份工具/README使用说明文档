# syqmtQQ798623 - 固定文件自动备份工具

按 `file_list.txt` 里列出的文件，每次运行时**只读复制**一份到 `backups/时间戳/` 目录，源文件不会被改动。

## 文件清单

| 文件 | 作用 |
| --- | --- |
| `syqmtQQ798623.py` | 主程序（Python，零第三方依赖） |
| `file_list.txt` | 要备份的文件路径列表 |

## 快速开始

1. 把需要备份的文件路径写进 `file_list.txt`，一行一个，例如：

```txt
"D:\国金证券QMT交易端\python\wave_detector.py"
"D:\国金证券QMT交易端\python\CKLBUY.py"
"D:\国金证券QMT交易端\bin.x64\a0stock_pool.csv"
```

2. 在 IDE 里打开 `syqmtQQ798623.py`，点运行（或命令行执行）：

```powershell
python syqmtQQ798623.py
```

3. 运行完成后，备份在：

```
backups/20260915_161501/
├── wave_detector.py.20260915_161501
├── CKLBUY.py.20260915_161501
├── a0stock_pool.csv.20260915_161501
└── report.json
```

## file_list.txt 规则

- `#` 开头的行为注释，会被忽略
- 空行会被忽略
- 一行一个文件绝对路径；加不加引号都可以
- 文件不存在不会报错，会记进 `report.json`

## 命令行参数

| 参数 | 作用 |
| --- | --- |
| （无） | 正常执行备份 |
| `--dry-run` | 只预览计划，不实际复制 |
| `--list` | 只打印列表里解析出的路径 |
| `--check` | 只校验文件是否存在/可读 |
| `--file my.txt` | 使用指定列表文件 |

## 提示

- **未开启本地编辑**：如果检测到某文件是 QMT 平台"未开启本地编辑"时导出的乱码内容，终端会提示「注意策略未开启本地编辑，请开启本地编辑后再备份」，且备份文件夹名会带上 `_未开启本地编辑` 后缀，方便识别。
- 程序只复制、不修改源文件，可放心反复运行。
